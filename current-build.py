# Build something for the general program here to test it out in isolation from organized folders/files.



#auto-line-printer main program


#classify buttons
class aButton:

    def __init__(self,text,trigger,position,size):
        self.text = text
        self.trigger = trigger
        self.position = position
        self.size = size

    def someMethod(self)
        return '{} {}'.format(self.var, self.var2) #returns string 'self.var self.var2'


button_1 = aButton()

button_1.text = 'text1'
button_1.trigger = 'trigger1'
